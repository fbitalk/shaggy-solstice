---
import Layout from '../layouts/Layout.astro';
---

<Layout title="关于我们 | 云之南导航">
	<section class="hero">
		<div class="container">
			<div class="hero-content">
				<h1 class="title">创意导航<br><span>与动画设计</span></h1>
				<p class="subtitle">使用Astro和GSAP创建的现代网站导航示例</p>
				<div class="cta-buttons">
					<a href="#learn-more" class="btn">了解更多</a>
					<a href="/contact" class="btn outline">联系我们</a>
				</div>
			</div>
			<div class="hero-image">
				<div class="shape shape-1"></div>
				<div class="shape shape-2"></div>
				<div class="shape shape-3"></div>
			</div>
		</div>
	</section>

	<section class="features" id="learn-more">
		<div class="container">
			<h2 class="section-title">主要特点</h2>
			<div class="feature-grid">
				<div class="feature-card">
					<div class="icon">🚀</div>
					<h3>高性能</h3>
					<p>Astro提供了令人难以置信的性能优势，确保您的网站加载速度非常快。</p>
				</div>
				<div class="feature-card">
					<div class="icon">✨</div>
					<h3>平滑动画</h3>
					<p>通过GSAP实现流畅的动画效果，提升用户体验和互动性。</p>
				</div>
				<div class="feature-card">
					<div class="icon">📱</div>
					<h3>响应式设计</h3>
					<p>完全适配各种屏幕大小，从手机到桌面设备均有良好表现。</p>
				</div>
				<div class="feature-card">
					<div class="icon">🔧</div>
					<h3>易于定制</h3>
					<p>简洁的代码结构使得网站易于维护和扩展。</p>
				</div>
			</div>
		</div>
	</section>

	<section class="about-content">
		<div class="container">
			<div class="content-wrapper">
				<div class="text-content">
					<h2>我们的故事</h2>
					<p>欢迎来到我们的导航网站演示！这是一个使用Astro和GSAP创建的现代网站示例，展示了如何构建具有流畅动画效果的导航界面。</p>
					<p>在这个项目中，我们展示了Astro的快速构建能力与GSAP强大的动画功能的结合，为用户提供了流畅且具有视觉吸引力的网站体验。</p>
					<p>这个示例网站可以作为您自己项目的起点，无论是个人作品集还是企业网站，都可以基于此进行定制和扩展。</p>
				</div>
				<div class="image-box">
					<div class="image-container"></div>
				</div>
			</div>

			<div class="tech-stack">
				<h2>技术栈</h2>
				<div class="tech-grid">
					<div class="tech-item">
						<h3>Astro</h3>
						<p>一个现代的静态网站构建工具，让您能够使用您喜欢的组件框架，同时提供闪电般的性能。</p>
					</div>
					<div class="tech-item">
						<h3>GSAP</h3>
						<p>专业级的JavaScript动画库，使构建复杂、高性能动画变得简单。</p>
					</div>
					<div class="tech-item">
						<h3>响应式设计</h3>
						<p>采用现代CSS技术，确保网站在所有设备上都有出色的表现。</p>
					</div>
					<div class="tech-item">
						<h3>性能优化</h3>
						<p>通过Astro的部分水合技术，确保最佳的加载速度和用户体验。</p>
					</div>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	.hero {
		min-height: calc(100vh - 80px);
		display: flex;
		align-items: center;
		position: relative;
		overflow: hidden;
	}

	.hero-content {
		max-width: 600px;
		z-index: 2;
		opacity: 0;
		transform: translateY(30px);
	}

	.title {
		font-size: 3.5rem;
		line-height: 1.2;
		margin-bottom: 1.5rem;
		background: linear-gradient(90deg, var(--primary-color), var(--secondary-color));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-clip: text;
	}

	.title span {
		display: block;
	}

	.subtitle {
		font-size: 1.25rem;
		margin-bottom: 2rem;
	}

	.cta-buttons {
		display: flex;
		gap: 1rem;
	}

	.btn.outline {
		background: transparent;
		border: 2px solid var(--primary-color);
		color: var(--primary-color);
	}

	.hero-image {
		position: absolute;
		right: 0;
		top: 50%;
		transform: translateY(-50%);
		width: 50%;
		height: 100%;
		z-index: 1;
	}

	.shape {
		position: absolute;
		border-radius: 50%;
		opacity: 0.7;
	}

	.shape-1 {
		width: 300px;
		height: 300px;
		background: linear-gradient(45deg, var(--primary-color), transparent);
		top: 20%;
		right: 10%;
		filter: blur(30px);
	}

	.shape-2 {
		width: 200px;
		height: 200px;
		background: linear-gradient(45deg, var(--secondary-color), transparent);
		top: 50%;
		right: 30%;
		filter: blur(20px);
	}

	.shape-3 {
		width: 150px;
		height: 150px;
		background: linear-gradient(45deg, #ff7e5f, transparent);
		top: 70%;
		right: 20%;
		filter: blur(15px);
	}

	.features {
		background-color: #fff;
		padding: 5rem 0;
	}

	.container {
		max-width: 1200px;
		margin: 0 auto;
		padding: 0 1rem;
		position: relative;
		z-index: 2;
	}

	.section-title {
		text-align: center;
		font-size: 2.5rem;
		margin-bottom: 3rem;
		opacity: 0;
		transform: translateY(30px);
	}

	.feature-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.feature-card {
		background: #fff;
		border-radius: 10px;
		padding: 2rem;
		box-shadow: 0 5px 20px rgba(0, 0, 0, 0.05);
		text-align: center;
		transition: all 0.3s ease;
		opacity: 0;
		transform: translateY(30px);
	}

	.feature-card:hover {
		transform: translateY(-10px);
		box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
	}

	.icon {
		font-size: 2.5rem;
		margin-bottom: 1rem;
	}

	.about-content {
		padding: 4rem 0;
		background-color: #f9f9f9;
	}

	.content-wrapper {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3rem;
		margin-bottom: 4rem;
		opacity: 0;
	}

	.text-content {
		line-height: 1.7;
	}

	.text-content p {
		margin-bottom: 1rem;
	}

	.image-box {
		position: relative;
	}

	.image-container {
		width: 100%;
		height: 100%;
		min-height: 300px;
		background: linear-gradient(45deg, var(--primary-color), var(--secondary-color));
		border-radius: 10px;
		position: relative;
		overflow: hidden;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.image-container::before {
		content: '🌟';
		font-size: 5rem;
		opacity: 0.2;
	}

	.tech-stack {
		opacity: 0;
	}

	.tech-stack h2 {
		margin-bottom: 2rem;
		text-align: center;
	}

	.tech-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
	}

	.tech-item {
		background: #fff;
		border-radius: 10px;
		padding: 2rem;
		box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
		opacity: 0;
		transform: translateY(30px);
	}

	@media (max-width: 768px) {
		.content-wrapper {
			grid-template-columns: 1fr;
		}

		.page-title {
			font-size: 2.5rem;
		}
		
		.hero-content {
			padding: 0 1rem;
			max-width: 100%;
			text-align: center;
		}

		.title {
			font-size: 2.5rem;
		}

		.cta-buttons {
			justify-content: center;
		}

		.hero-image {
			opacity: 0.3;
			width: 100%;
		}
	}
</style>

<script>
	import { gsap } from 'gsap';
	import { ScrollTrigger } from 'gsap/ScrollTrigger';

	document.addEventListener('DOMContentLoaded', () => {
		gsap.registerPlugin(ScrollTrigger);
		
		// 页面加载动画
		const tl = gsap.timeline();
		
		tl.to('.hero-content', {
			opacity: 1,
			y: 0,
			duration: 1,
			ease: 'power3.out'
		});
		
		// 形状动画
		gsap.to('.shape-1', {
			x: -30,
			y: 30,
			duration: 4,
			repeat: -1,
			yoyo: true,
			ease: 'sine.inOut'
		});
		
		gsap.to('.shape-2', {
			x: 30,
			y: -30,
			duration: 5,
			repeat: -1,
			yoyo: true,
			ease: 'sine.inOut'
		});
		
		gsap.to('.shape-3', {
			x: -20,
			y: -20,
			duration: 4.5,
			repeat: -1,
			yoyo: true,
			ease: 'sine.inOut'
		});
		
		// 滚动触发动画
		gsap.to('.section-title', {
			scrollTrigger: {
				trigger: '.section-title',
				start: 'top 80%',
			},
			opacity: 1,
			y: 0,
			duration: 1,
			ease: 'power3.out'
		});
		
		// 特性卡片动画
		const cards = document.querySelectorAll('.feature-card');
		cards.forEach((card, index) => {
			gsap.to(card, {
				scrollTrigger: {
					trigger: card,
					start: 'top 80%',
				},
				opacity: 1,
				y: 0,
				duration: 0.8,
				delay: index * 0.2,
				ease: 'power3.out'
			});
		});
		
		// 内容区域动画
		gsap.to('.content-wrapper', {
			scrollTrigger: {
				trigger: '.content-wrapper',
				start: 'top 80%',
			},
			opacity: 1,
			duration: 1,
			ease: 'power3.out'
		});
		
		// 技术栈动画
		gsap.to('.tech-stack', {
			scrollTrigger: {
				trigger: '.tech-stack',
				start: 'top 80%',
			},
			opacity: 1,
			duration: 1,
			ease: 'power3.out'
		});
		
		// 技术项目动画
		const techItems = document.querySelectorAll('.tech-item');
		techItems.forEach((item, index) => {
			gsap.to(item, {
				scrollTrigger: {
					trigger: item,
					start: 'top 85%',
				},
				opacity: 1,
				y: 0,
				duration: 0.8,
				delay: index * 0.15,
				ease: 'power3.out'
			});
		});
	});
</script> 